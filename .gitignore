node_modules/
out/
release/
dist/
.DS_Store
*.log
.env
.env.local
.env.*.local
.vite-cache/

# Editor / IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db

# TypeScript incremental build info
*.tsbuildinfo

# Icon staging dir (intermediate; final .icns is committed)
assets/icon.iconset/

# Lockfiles from package managers we don't use
package-lock.json
yarn.lock

# Personal context (local only, never shared)
.ghost-personal.md

# Auto-generated ghost skills — every new <skill>/ subdir stays local, in case
# selectors, URLs, or notes captured during a real task contain sensitive data.
# Already-tracked skills (e.g. facebook-search-friend) are unaffected — gitignore
# does not untrack files that are already in the repo. Index file is intentionally
# kept tracked so the project ships with a usable agent skeleton.
.claude/skills/*/

# Claude Code runtime state (session-local locks for scheduled tasks etc.)
.claude/scheduled_tasks.lock

# Playwright
test-results/

# Personal Obsidian vault — notes, journals, and scripts that may contain PII.
# Lives inside the project root for convenience but never ships with the repo.
Obsidian/
